#include <string.h>
#include <stdlib.h>
int main()
{
    int number;
    int loop;
    printf("Is it prime or not ?\n==>");
    scanf("%d",&number);
    for(loop=2;loop<number;dongu++)
    {

          if(number%loop==0)
          {
              printf("It's not a prime number!");
              break;
          }
    }
        if(number==loop)
                {
                    printf("PRIME");

                }
}
